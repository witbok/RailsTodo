<div class="page-header">
  <h1><span class="glyphicon glyphicon-pencil"></span> New List</h1>
</div>

<%= bootstrap_form_for @list do |l| %>
	<%= l.text_field :name %>
	<%= l.text_area :description %>
	<%= l.submit "Create New List", {class: "btn btn-primary"} %>
<% end %>